{% extends 'base.html.twig' %}

{% block title %}{{ movie.title }}{% endblock %}

{% block body %}
    </div>

    {#<div class="movie__hero_container" style="background:url({{ movie.url }})">#}
    {#<img src="{{ movie.url }}" alt="">#}

    {#<div class="movie__hero_content">#}
    {#<h1 class="movie__hero_heading">{{ movie.title }}</h1>#}
    {#</div>#}
    {#</div>#}



    <section class="movie">


        <!-- Full Page Intro -->
        <div class="view jarallax movie__hero__container" data-jarallax='{"speed": 0.2}'
             style="background-image: url({{ asset('uploads/images/' ~ movie.image)  }}); background-repeat: no-repeat; background-size: cover; background-position: center center;">
            <!-- Mask & flexbox options-->
            <div class="mask rgba-black-light">

            </div>
        </div>
        <!-- Mask & flexbox options-->


        <!-- Main navigation -->

        <div class="container">
            <!--Grid row-->
            <div class="row">
                <!--Grid column-->
                <div class="col-md-12 text-center">
                    <h1 class="h1-reponsive  text-uppercase font-weight-bold mb-0 pt-md-5 pt-5 wow fadeInDown"
                        data-wow-delay="0.3s"><strong>{{ movie.title }}</strong></h1>
                    <h5>{{ movie.category }}</h5>
                    <hr class="hr-dark my-4 wow fadeInDown" data-wow-delay="0.4s">
                    {#<h5 class="text-uppercase mb-4 wow fadeInDown" data-wow-delay="0.4s"><strong>Photography & design</strong></h5>#}
                </div>
                <!--Grid column-->
            </div>
            <!--Grid row-->

        <p>{{ movie.description }}</p>

        </div>

        <!-- Full Page Intro -->
    </section>

{% endblock %}