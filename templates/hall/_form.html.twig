    {{ form_widget(form) }}
    {{ form_errors(form.number_of_seats_v) }}
    {{ form_label(form.number_of_seats_v) }}
    {{ form_widget(form.number_of_seats_v) }}

    <button class="btn">{{ button_label|default('Save') }}</button>
{#{{ form_end() }}#}