{% extends 'base.html.twig' %}

{% block title %}Hall{% endblock %}

{% block body %}
    <section class="admin_area">
        <h1>Sala</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hall.id }}</td>
            </tr>
            <tr>
                <th>Number_of_seats_v</th>

                <td>{{ hall.getNumberOfSeatsV }}</td>
            </tr>
            <tr>
                <th>Number_of_seats_h</th>
                <td>{{ hall.getNumberOfSeatsH }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('hall_index') }}">Powrot to listy</a>

        {#<a href="{{ path('hall_edit', {'id': hall.id}) }}">Edytuj</a>#}

        {{ include('hall/_delete_form.html.twig') }}
    </section>
{% endblock %}