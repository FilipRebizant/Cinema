{% extends 'base.html.twig' %}

{% block title %}Hall{% endblock %}

{% block body %}
    <h1>Hall</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hall.id }}</td>
            </tr>
            <tr>
                <th>Number_of_seats_v</th>
                <td>{{ hall.number_of_seats_v }}</td>
            </tr>
            <tr>
                <th>Number_of_seats_h</th>
                <td>{{ hall.number_of_seats_h }}</td>
            </tr>
            <tr>
                <th>Hall_number</th>
                <td>{{ hall.hall_number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hall_index') }}">back to list</a>

    <a href="{{ path('hall_edit', {'id': hall.id}) }}">edit</a>

    {{ include('hall/_delete_form.html.twig') }}
{% endblock %}